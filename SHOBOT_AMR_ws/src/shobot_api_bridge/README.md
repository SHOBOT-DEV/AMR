# shobot_api_bridge

API bridge to expose robot interfaces to external systems (e.g., REST/WebSocket).

## Usage
Run node to provide bridge services; configure endpoints/topics in code as needed. Intended for non-ROS clients to command/query the robot.
