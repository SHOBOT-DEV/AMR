# shobot_mission_handler

Handles individual mission/task execution (used by mission control).

## Usage
Triggered by `shobot_mission_control` to perform task-specific steps (e.g., goal handling, docking). Configure behavior and topics in code/params as needed for your missions.
