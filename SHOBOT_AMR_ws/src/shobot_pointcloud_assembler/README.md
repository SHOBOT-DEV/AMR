# shobot_pointcloud_assembler

Assembles incoming point clouds into a combined cloud for downstream processing.

## Usage
Key params: input cloud topics list, output topic, frame; configure in launch/parameters to stitch multiple sensors into one point cloud.
