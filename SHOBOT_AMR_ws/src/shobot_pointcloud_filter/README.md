# shobot_pointcloud_filter

Filters 3D point clouds (e.g., passthrough/outlier removal).

## Usage
Key params: input/output topics; filter settings (passthrough limits, voxel size, etc.—configure in launch/parameters).
