# shobot_pointcloud_to_laserscan

Converts 3D point clouds into 2D LaserScan messages for navigation/safety.

## Usage
Key params: input point cloud topic, output scan topic, min/max ranges and angle settings (configure in launch/parameters).
