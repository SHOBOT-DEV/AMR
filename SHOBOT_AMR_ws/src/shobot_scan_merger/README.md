# shobot_scan_merger

Merges multiple LaserScan topics into one scan by taking the minimum range per index.

## Usage
Key params: list of input scan topics and output scan topic (configure in node/launch). Publishes a single merged scan that combines all inputs.
