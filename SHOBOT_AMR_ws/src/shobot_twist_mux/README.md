# shobot_twist_mux

Multiplexes velocity commands from sources like safety, teleop, and navigation.

## Usage
Configure source priorities, timeouts, and topics in parameters (see bring-up launch). Outputs a selected `/cmd_vel`.
